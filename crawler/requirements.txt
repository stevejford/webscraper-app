# Requirements for Crawl4AI
# Core installation - this is what gets installed first
crawl4ai>=0.6.0

# Optional dependencies (uncomment if needed)
# crawl4ai[torch]      # For PyTorch features (text clustering, semantic analysis)
# crawl4ai[transformer] # For Transformer features (advanced NLP)
# crawl4ai[all]        # For all features (comprehensive)

# Additional dependencies for our web scraper integration
aiohttp>=3.9.0
aiofiles>=23.0.0
beautifulsoup4>=4.12.0
pydantic>=2.0.0